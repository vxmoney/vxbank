const configValues = {
  useEmulator: false,
  protocol: "http",
  baseUrl: "localhost",
  port: 8080,
  frontendPort:3000,
  frontendBaseUrl: "localhost",
  frontendProtocol:"http"
};
export default configValues;
